{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter=shadcn build && cd apps/www && pnpm run build:registry && pnpm run build",
  "outputDirectory": ".next",
  "installCommand": "echo 'Dependencies installed in buildCommand'",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- .",
  "functions": {
    "app/**": {
      "maxDuration": 30
    }
  },
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
