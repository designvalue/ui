{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && cd packages/shadcn && npx tsup && cd ../../apps/www && contentlayer2 build && pnpm run build:registry && next build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- .",
  "functions": {
    "app/**": {
      "maxDuration": 30
    }
  },
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
